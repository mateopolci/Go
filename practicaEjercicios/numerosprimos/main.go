/*
Escriba un programa para buscar todos los números primos inferiores a 20. 
Un número primo es cualquier número mayor que 1 que se puede dividir uniformemente solo por sí mismo 
y por 1. "Dividirse uniformemente" significa que no hay resto después de la operación de división.

Al igual que la mayoría de los lenguajes de programación, Go proporciona una manera de comprobar 
si una operación de división genera un resto. Podemos usar el operador de módulo % (signo de porcentaje).

En este ejercicio, actualizará una función llamada findprimes para comprobar si un número es primo. 
La función tiene un argumento entero y devuelve un valor booleano. La función comprueba si el número de 
entrada es primo comprobando si hay un resto. Si el número es un número primo, la función devuelve true.

Use el siguiente fragmento de código como punto de partida y reemplace 
todas las instancias de ?? con la sintaxis correcta:
*/

package main

func main(){

}